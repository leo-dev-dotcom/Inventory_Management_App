import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#fff',
    flex: 1,
  },

  welcomeText: {
    // fontFamily: '700',
    fontWeight: '700',
    fontSize: 20,
    color: '#000',
  },
});
