import {colorConfig} from './setup.color';
export const colors = {
  container: colorConfig.container,
  black: colorConfig.black,
  white: colorConfig.white,
  primary: colorConfig.primary,
  primaryText: colorConfig.primaryText,
  secondary: colorConfig.secondary,
  secondaryText: colorConfig.secondaryText,
  primaryDisable: colorConfig.primaryDisable,
};
